export declare const cloudflareWorkerConfig: {
    dynamicMinIntervalMs: number;
    dynamicMaxRetries: number;
    dynamicCircuitBreakerMs: number;
    dynamicFailFast: boolean;
    dynamicTimeoutMs: number;
};
export declare const merklFetchConfig: {
    maxRetries: number;
    baseDelayMs: number;
    maxDelayMs: number;
};
export declare const coingeckoFetchConfig: {
    maxRetries: number;
    baseDelayMs: number;
    maxDelayMs: number;
    rateLimitMinWaitSeconds: number;
    minRequestIntervalMs: number;
};
export declare const meritKeyAliases: Record<string, string>;
//# sourceMappingURL=config.d.ts.map